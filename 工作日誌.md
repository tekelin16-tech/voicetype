# VoiceType 工作日誌

## 2026-09-17 ～ 09-23　建置到上線

一個 macOS 語音聽寫工具（Typeless 的自建替代品），加上 Android 版與跨裝置詞彙表同步。

---

## 最重要的一件事：DeepSeek 沒有語音辨識 API

使用者原本的想法是「接 DeepSeek API 來做語音辨識」。但 DeepSeek 只有文字模型
（`deepseek-flash` / `deepseek-v4-pro`），**沒有 audio transcription 端點**。

所以架構是兩段：

```
whisper.cpp 本機聽  →  DeepSeek 修稿  →  送進游標位置
```

DeepSeek 負責的那半段（補標點、去贅字、簡轉繁台灣用語、依情境改語氣）才是
真正的價值，也是唯一能跨平台原封不動搬走的東西。

**下次遇到「接某某 API 做語音辨識」，先確認那家有沒有 audio 端點。**

---

## 三個專案的位置

| 專案 | 原始碼 | 上線位置 |
|---|---|---|
| macOS 版 | `~/Desktop/claude日常/voicetype`（即 git repo） | `~/.local/share/voicetype`、`~/.hammerspoon` |
| Android 版 | `~/Desktop/claude日常/voicetype-android` | Pixel 8a（debug APK） |
| 同步服務 | `~/Desktop/claude日常/voicetype-sync` | server1 `/opt/voicetype-sync` |

- GitHub：https://github.com/tekelin16-tech/voicetype （公開，授權是「保留所有權利」）
- 安裝頁：https://tekelin16-tech.github.io/voicetype
- 一行安裝：`curl -fsSL https://tekelin16-tech.github.io/voicetype/install.sh | bash`

---

## macOS 版的架構決定

**錄音不用 ffmpeg，用自建的 `VoiceTypeRec.app`**
Homebrew 的 ffmpeg 是 ad-hoc 簽章、沒有 TeamIdentifier，macOS 不給它麥克風——
而且是**靜默拒絕**：exit 0、檔案照產生，內容卻是一串零（-91dB）。
母程序（Hammerspoon、終端機）在系統設定的麥克風清單裡打勾也沒用，授權不沿程序鏈傳。
解法是自己包一個有 `Info.plist` + `NSMicrophoneUsageDescription` 的 app bundle，
用 Swift + AVFoundation 錄音。ffmpeg 只留著轉檔（那不需要麥克風權限）。

**辨識模型 large-v3-turbo + whisper-server 常駐**
實測 11.7 秒語音：large-v3 3.6s、medium 1.7s、turbo 1.4s（品質等同 large-v3）。
搭 `whisper-server` 把模型留在記憶體 → **0.8 秒**。

**貼上用 App 自己的「編輯→貼上」選單，不是模擬按鍵**
合成 ⌘V 在 Electron App（Claude、Slack、Notion）會「post 成功但沒作用」，
記錄看起來像成功實際上文字沒進去。`app:selectMenuItem({"Edit","Paste"})` 走輔助功能
API，等於使用者真的去點選單，一定會處理。選單名稱跟系統語言走，Edit/Paste 與
編輯/貼上 都要試。

**貼上目標判斷用反向黑名單**
Claude 桌面版回報 `AXFocusedUIElement = nil`，Notion 之類回報 `AXGroup`。
用「必須是 AXTextField」的白名單會拒絕貼進一大堆正常的輸入框。
現在只有密碼欄位會拒絕；判斷不出來就照貼，但**不還原剪貼簿**（文字留著當保險）。

---

## 模型掛掉的三層防護

DeepSeek 的 flash 層會整層塞車（回 503 或直接不回應）。

1. 主模型不通 → 切備援，記下時間，**冷卻期內直接走備援**不再試主模型
   （預設 5 分鐘。flash 是間歇性塞車，一小時內可能失敗三次但每次幾分鐘就恢復，
   設 4 小時等於一次失敗就整個下午用慢的。實測 flash 1.0s、pro 2.5s）
2. 冷卻期滿自動回頭試，通了就切回來並通知
3. 兩個都不通 → **暫停呼叫 10 分鐘**，期間直接秒出未整理的原文（0.08 秒）

逾時分開：主模型 8 秒（有備援可退）、備援 25 秒（最後一道）。
設定視窗最上面有狀態卡（綠／黃／紅燈）＋「測試連線」按鈕＋「立即重試」。
指令列：`vt.sh model-status`、`vt.sh model-test`、`vt.sh model-reset`。

診斷時注意：
- `/user/balance` 會正常回應而 `/chat/completions` 掛住，別誤判成帳號或網路問題
- **測試連線要用跟真實口述一樣大的請求**。小請求（`"ok"` + max_tokens 8）在
  flash 塞車時照樣擠得進去，手動測永遠正常，但真實請求（系統指令＋詞彙表＋
  前後文約 1500 字元）會失敗。測試跟真實不一樣就等於沒測。

---

## 詞彙表

一份清單三個用途：whisper 熱詞（取前 40）、DeepSeek 的專有名詞清單、帶 `→` 的強制替換。
存在 `~/.config/voicetype/corrections.txt`。

原本拆成「熱詞表」和「修正字典」兩個欄位，結果兩邊重疊又各有遺漏，
使用者根本不知道該填哪邊——**合併成一個，而且放在設定最上面**。

**編輯歷史紀錄會順便學會**：存檔時比對前後差異（字元層級 LCS），找出換掉哪個詞，
跳出來問要不要記住。規則自動帶前後文——不然「林國祥→林國翔」會產生規則「祥→翔」，
以後講「吉祥」就變「吉翔」，比原本的錯還糟。

**`scan-vocab.py`**：從使用者寫過的 md／txt 挖專有名詞。
不做鍵盤攔截（那會錄下密碼、私訊、金融資料）。
實測在 155 個檔案、82 萬字元裡挖出 Seedance、Kling、Veo、Sora、方舟協會等。

---

## 同步服務

`https://voice.arkapp.pw`（server1，Apache 反向代理到 127.0.0.1:8210，
gunicorn + Flask + SQLite，systemd `voicetype-sync`）

認證用**帳號碼**不是帳密——沒有密碼就沒有外洩、忘記、暴力破解的問題。
碼以 SHA-256 雜湊存放，資料庫整個被拿走也拿不到任何人的碼。

管理：`vt-admin add/list/revoke/show`（在 server1 上）
Mac 端：`vt.sh sync` / `vt.sh sync-login`，帳號碼存 Keychain `voicetype-sync-token`

**合併用聯集，不是「後寫的贏」**。兩台裝置各自加的詞都保留——
覆蓋式同步會讓手機上加的詞被電腦默默吃掉。代價是刪除不會同步。

部署踩雷：
- `*.arkapp.pw` 萬用字元指向另一台主機（PHP 7.4，不是 server1 的 8.2），
  新子網域要自己加 A 記錄，**設成 DNS only 灰雲**——走 CF proxy 會讓 certbot
  卡在雞生蛋問題（CF 把驗證轉成 HTTPS，但 origin 還沒有該網域的 HTTPS vhost）
- `ProxyPass /` 會把 `.well-known/acme-challenge` 也吃掉，
  要先 `ProxyPass /.well-known/ !`

---

## Android 版

`~/Desktop/claude日常/voicetype-android`，Kotlin 輸入法（IME）。

```
麥克風 → Android 內建 SpeechRecognizer → DeepSeek 修稿 → commitText 直接送進欄位
```

**不是移植是重寫**，只有 prompt 和詞彙表邏輯共用。做成輸入法是因為 Android 沒有
全域熱鍵，而且 `commitText` 不用剪貼簿——macOS 版那整類貼上問題在這裡不存在。

編譯：`export JAVA_HOME=/opt/homebrew/opt/openjdk@17`，`./install-to-phone.sh`
第一次約 40 分鐘（下載 300MB）。

Android 特有的坑：
- 輸入法不能自己跳權限對話框，要導到 Activity 去要
- Android 11 以上要宣告 `<queries>` 才查得到語音辨識服務，
  否則 `isRecognitionAvailable()` 永遠 false 且不報錯
- 邊到邊：導覽列會蓋住底部按鈕，insets 要套在最外層 ScrollView
  （套在裡層 LinearLayout 收不到，insets 由外往內派送）
- 密碼類型的 EditText 用 uiautomator 讀 text 會拿到遮蔽值，別拿來驗證儲存成功

**狀態：能錄能辨識能修稿，詞彙表同步的程式碼寫完但沒在真機測過**（手機拔掉了）。

---

## 這次最花時間的三個 bug

都有同一個特徵：**從終端機跑完全正常，只在 Hammerspoon 裡出錯，而且沒有任何錯誤訊息。**

**一、`hs.task` 輸出超過約 16KB 就不可靠**
實測 limit=60（13KB）正常、limit=80（20KB）拿到零 bytes 而且 exit=0、stderr 空的。
即使帶了串流回呼也一樣。症狀是歷史清單整個消失。
**大量資料一律走檔案**：shell 寫檔、Lua 讀檔。

**二、`set -o pipefail` 加 `head` 截斷的競態**
`head -n 80` 讀夠就結束，上游 `tail` 收到 SIGPIPE 而失敗，pipefail 讓整條管線
算失敗 → 走進 `|| jq -nc '[]'` 回傳空陣列。從終端機跑時 tail 常常剛好先寫完。
要截斷讓 jq 自己切（`.[0:80]`），不要用 head 提早關管線。

**三、用位置切片改程式碼刪掉了夾在中間的東西**
做串流回呼時用 `s[s.index(A):s.index(B)]` 替換，`pasteTarget` 和 `TEXT_ROLES`
剛好夾在中間被一起刪掉。`pasteOut` 第一行就呼叫不存在的函式直接死。
**所有檢查都顯示正常**：語法檢查過（呼叫 nil 是執行期錯誤）、模組載入成功、
錯誤在 hs.task 回呼裡被吞掉。

---

## 診斷方式（重要）

載入了 `hs.ipc` 並加了 `vtDebug` 接口，**不重啟就能檢查執行中的狀態**：

```bash
hs -c 'return vtDebug.state()'        # 熱鍵數、貼上函式在不在、目前狀態
hs -c 'return vtDebug.js([[...]])'    # 在設定視窗裡執行 JS，結果寫 /tmp/vtdebug.txt
```

這個是後來才加的。在那之前為了診斷反覆重啟 Hammerspoon 十幾次，
每次都讓使用者的熱鍵失效幾秒、正在錄的音被中斷——**他當時正在用**。
以後不要在使用者正在用的時候反覆重啟。

記錄在 `~/.cache/voicetype/vt.log`，Hammerspoon 端的事件也寫進同一份，
時間軸可以對照。每次貼上都會記錄走選單還是模擬按鍵、送給哪個 App。

---

## 還沒做完

1. **Android 詞彙表同步沒在真機測過** — 插上手機兩分鐘就能驗完
2. **設定畫面某處無法置中** — 使用者提過但說先不理
3. **Fn 熱鍵** — 探針沒收到任何 fn 旗標的事件，不確定 macOS 是否送得出來。
   `⌥Z` 這類單修飾鍵組合已驗證可用，手感接近，可能不值得為 Fn 增加複雜度

## 之後可能要做

- **前後文**：游標前已有的文字能大幅改善同音字判斷（商業工具的主要手段），
  但那是新的隱私外洩，預設關閉。Electron App 讀不到（`role=nil`）。
  最近三句口述當背景已預設開啟（那些本來就送過 DeepSeek）
- **跨裝置同步的刪除語意**：目前聯集合併，刪除不會同步
- 使用者提過想商業化。授權已先收緊成「保留所有權利」，要放寬容易、收回不可能。
  真要商業化＋支援 Windows，等於重寫成 Electron／Tauri 獨立 App
